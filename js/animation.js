document.addEventListener("DOMContentLoaded", function () {
    const helloWorld = document.getElementById("hello-world");
    
    // Adiciona a classe 'visible' para iniciar a animação
    helloWorld.classList.add("visible");
});
